name: Issue Comments

on:
  issues:
    types: [opened, reopened]
  workflow_dispatch:

jobs:
  print-comment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.8'
          architecture: 'x64'
      - run: pip install -r requirements.txt
      - run: python GitHub.py ${{ github.event.issue_url }} ${{ secrets.GITHUB_TOKEN }}
